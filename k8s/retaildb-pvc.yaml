apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: retaildb-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: "" 
  volumeName: retaildb-pv
